stages:
  - run-pipeline

elife-pipeline:
  only:
    refs:
      - main
  stage: run-pipeline
  trigger:
    include:
      - local: .gitlab-ci.elife.yml
    strategy: depend

coko-pipeline:
  stage: run-pipeline
  trigger:
    include:
      - local: .gitlab-ci.coko.yml
    strategy: depend

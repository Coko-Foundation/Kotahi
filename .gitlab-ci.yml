image: node:7.9

stages:
  - install
  - lint
  - bootstrap
  - test

install:
  stage: install
  script: npm install
  artifacts:
    paths:
      - node_modules

lint:
  stage: lint
  script: npm run lint

#bootstrap:
#  stage: bootstrap
#  script: npm run bootstrap
#  artifacts:
#    paths:
#      - packages/*/node_modules
#
#test:
#  stage: test
#  script: npm run test

# TODO: start?
# TODO: build
# TODO: deploy

image: node:8

install:
  stage: build
  script: yarn
  artifacts:
    expire_in: 1h
    paths:
      - node_modules
      - packages/*/node_modules

lint:
  script: npm run lint

test:
  script: npm run test

# TODO: start?
# TODO: build
# TODO: deploy
